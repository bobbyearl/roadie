# Snapshot API URL (Cloudflare Worker)
# Leave empty to disable snapshot uploads (local-only mode)
# Set to your Worker URL for persistent sharing
VITE_SNAPSHOT_API=
# Example: VITE_SNAPSHOT_API=https://roadie-snapshots.<your-subdomain>.workers.dev
