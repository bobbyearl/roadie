"""Camera data fetchers for Roadie.

Each fetcher module exports a `fetch()` function that returns a list of camera dicts
in the standard format: {id, name, route, jurisdiction, lat, lng, image_url, video_url}
"""
